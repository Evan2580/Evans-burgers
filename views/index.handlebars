<ul class="burger_list">
  {{#each burgers}}
  {{#if this.devoured}}
  <li>{{this.burgerName}}</li>
  {{/if}}

  {{/each}}
</ul>
<ul class="devoured_list">
  {{#each burgers}}
  {{#unless this.devoured}}
  <li>{{this.burgerName}}</li>
  {{/unless}}
  {{/each}}
</ul>
